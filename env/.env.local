# This file includes environment variables that can be committed to git. It's gitignored by default because it represents your local development environment.

# Built-in environment variables
TEAMSFX_ENV=local
APP_NAME_SUFFIX=local

# Generated during provision, you can also add your own variables.
#joe add hmm hit error on apppackage play.. need this since in repair.yml
OPENAPI_SERVER_URL=http://localhost:7071

# joe hmm
# ai Step 2: Add only this line (placeholder first)
# This will silence the current validation error immediately.
# You can replace it with the real value later once OAuth is configured.
# OAUTH2AUTHCODE_CONFIGURATION_ID=TEMP_PLACEHOLDER
OAUTH2AUTHCODE_CONFIGURATION_ID=22222222-2222-2222-2222-22222222222



# joe hmm ai chat
# If you want it to actually authenticate (not just run)
# You must do one of these:
# Option A — Real OAuth (recommended)
# ---ai
# Go to Microsoft 365 Developer Portal
# Open your Agent / App
# Configure OAuth → Authorization Code
# Copy the Configuration ID
# Paste it into .env.local
# ---ai
# Option B — Skip OAuth while learning
# If you’re just testing plumbing:
# ---ai
# Comment out OAuth in ai-plugin.json
# Use anonymous or API key auth
# Re-enable OAuth later

# Joe .. ai:  .. Fake but well‑formed GUIDs for local packaging only
AAD_APP_TENANT_ID=00000000-0000-0000-0000-000000000000
AAD_APP_CLIENT_ID=11111111-1111-1111-1111-111111111111
# ai: These satisfy:
# repair.yml   ( hmm has oauth2 )
# ai-plugin.json
# OAuth security scheme validation

# Joe ..ai hmm to get around a fake deploy to Azure (though ai said all done) .. 
# hmm a stub just to see depoloy not error out?
API_FUNCTION_RESOURCE_ID=/subscriptions/00000000-0000-0000-0000-000000000000/resourceGroups/fakeRG/providers/Microsoft.Web/sites/fakeFunctionApp


